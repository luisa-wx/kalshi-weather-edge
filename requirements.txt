# WX Sniper v4.6 Dependencies

# API and HTTP
requests>=2.28.0

# Cryptography for Kalshi API auth
cryptography>=40.0.0

# SMS via Twilio (optional)
twilio>=8.0.0

# Web server for webhooks (optional, using built-in http.server)
flask>=2.3.0

# Scheduling
schedule>=1.2.0

# Timezone handling (zoneinfo is built-in Python 3.9+, tzdata provides the data)
tzdata>=2024.1

# Optional but useful
python-dotenv>=1.0.0

# Production WSGI server (optional for dashboard)
gunicorn>=21.0.0

# AWS Integration (optional - for Secrets Manager and CloudWatch)
boto3>=1.28.0
